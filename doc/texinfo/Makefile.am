## automake Makefile for doc/texinfo

info_TEXINFOS = epeg.texi

epeg_TEXINFOS = version.texi

## override the automake default temporarily until I fix the issues with it
TEXI2DVI = texi2dvi --verbose --batch -I . -I ../latex 2>&1
